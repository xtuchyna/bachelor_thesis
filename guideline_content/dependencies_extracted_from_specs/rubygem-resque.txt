/usr/bin/hostname
/usr/bin/ps
redis
ruby(release)
rubygem(minitest)
rubygem(mocha)
rubygem(mono_logger) < 2
rubygem(mono_logger) >= 1.0
rubygem(multi_json) < 2
rubygem(multi_json) >= 1.0
rubygem(rack-test)
rubygem(redis-namespace) < 2
rubygem(redis-namespace) >= 1.3
rubygem(sinatra) >= 0.9.2
rubygems-devel
